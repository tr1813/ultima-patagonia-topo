encoding utf-8

layout local
  debug off
  map-header 0 0 off
  # symbol-hide group cave-centreline
endlayout

source "PE29.th"

export model -fmt survex -o "PE29.th"

# export map -proj plan -layout local -o "230211_perte2lacs-plan.pdf"

# export map -proj extended -layout local -o "230211_perte2lacs-ee.pdf"