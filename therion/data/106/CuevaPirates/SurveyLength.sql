-- SQLite
SELECT c.EXPLO_DATE,s.FULL_NAME,c.LENGTH, c.DUPLICATE_LENGTH,p.SURNAME 
FROM SURVEY as s, CENTRELINE as c, PERSON as p, EXPLO as e
WHERE c.LENGTH > 0 AND c.SURVEY_ID == s.ID AND e.PERSON_ID == p.ID AND e.CENTRELINE_ID == c.ID
ORDER BY c.EXPLO_DATE;